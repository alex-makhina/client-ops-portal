@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        var loginUri = Navigation.ToAbsoluteUri("authentication/login").ToString();
        Navigation.NavigateTo(loginUri, forceLoad: true);
    }
}
