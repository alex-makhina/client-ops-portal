{
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5432;Database=addresses;Username=addr;Password=addr_secret;Timeout=15;CommandTimeout=120"
  },
  "Elasticsearch": {
    "Uri": "http://localhost:9200",
    "IndexName": "addresses",
    "RequestTimeout": "00:01:00",
    "BulkBatchSize": 2000
  },
  "MappingFile": "config/elasticsearch/address-mappings.json"
}
