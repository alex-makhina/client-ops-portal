{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5432;Database=addresses;Username=addr;Password=addr_secret;Timeout=15;CommandTimeout=60"
  },

  "Elasticsearch": {
    "Uri": "http://localhost:9200",
    "IndexName": "addresses",
    "RequestTimeout": "00:00:30"
  }
}
